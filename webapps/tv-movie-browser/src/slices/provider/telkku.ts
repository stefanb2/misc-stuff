const program = async (day: Day, selectorId: string) => {
  return []
}

const selectors = async () => {
  return {}
}

const provider = {
  name: 'telkku',
  program,
  selectors,
}

export default provider
